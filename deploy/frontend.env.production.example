# Frontend (Next.js) — скопируйте в frontend/.env.production на сервере
# Замените YOUR_DOMAIN на ваш домен (например: tayga-dev.ru или api.tayga-dev.ru)

# Публичный URL бэкенда (тот же домен, что и сайт — nginx проксирует /api и /uploads)
# Без слэша в конце. Для HTTPS: https://YOUR_DOMAIN
NEXT_PUBLIC_STRAPI_API_URL=https://YOUR_DOMAIN
NEXT_PUBLIC_STRAPI_MEDIA_URL=https://YOUR_DOMAIN
